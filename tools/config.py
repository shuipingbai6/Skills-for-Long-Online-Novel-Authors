#!/usr/bin/env python3
"""
共享配置模块

集中定义各工具脚本中重复使用的常量，避免分散定义导致不一致。
"""

from __future__ import annotations

# ---------------------------------------------------------------------------
# 文件大小限制
# ---------------------------------------------------------------------------

# 通用文件大小上限（评论等文本文件，对应 comment_parser 的 50MB）
MAX_FILE_SIZE = 50 * 1024 * 1024

# 小说 txt 文件大小上限（对应 novel_parser 的 100MB）
MAX_NOVEL_FILE_SIZE = 100 * 1024 * 1024

# epub 文件大小上限（对应 epub_parser 的 100MB）
MAX_EPUB_SIZE = 100 * 1024 * 1024

# ---------------------------------------------------------------------------
# 格式化参数
# ---------------------------------------------------------------------------

# 分隔线宽度
SEPARATOR_WIDTH = 60

# 默认预览长度（字数）
DEFAULT_PREVIEW_LENGTH = 500

# 默认目标章节长度（字数，用于无章节标记时的段落分割）
DEFAULT_TARGET_CHAPTER_LENGTH = 3000

# ---------------------------------------------------------------------------
# Skill 文件列表
# ---------------------------------------------------------------------------

# 版本管理需要备份的 Skill 文件集合（含 meta.json）
SKILL_FILES = (
    "SKILL.md",
    "writing.md",
    "author_persona.md",
    "writing_skill.md",
    "persona_skill.md",
    "meta.json",
)

# ---------------------------------------------------------------------------
# Slug 校验
# ---------------------------------------------------------------------------

# slug 正则模式：仅允许小写字母、数字和连字符
SLUG_RE_PATTERN = r'^[a-z0-9]+(?:-[a-z0-9]+)*$'

# ---------------------------------------------------------------------------
# 编码回退顺序
# ---------------------------------------------------------------------------

# 文件读取时的编码尝试顺序
ENCODING_FALLBACK_ORDER = ('utf-8-sig', 'utf-8', 'gb18030', 'gbk')

# ---------------------------------------------------------------------------
# 版本与数量限制
# ---------------------------------------------------------------------------

# 最大保留版本数
MAX_VERSIONS = 10

# 评论数量上限（DoS 防护）
MAX_COMMENT_COUNT = 100_000
